{
  "name": "@hyacine/contract",
  "version": "0.1.0",
  "description": "hyacine 三端契约：zod schemas + 推导类型 + typed client（唯一真相）",
  "files": [
    "dist",
    "README.md"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsdown src/index.ts -d dist --target node24 --format esm --dts --clean --unbundle",
    "typecheck": "oxlint -A all --type-aware --type-check .",
    "typecheck:tsc": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "pinyin-pro": "^3.29.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsdown": "^0.14.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=24"
  }
}
